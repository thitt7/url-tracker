{
  email {env.CADDY_EMAIL}
}

{env.STAGING_DOMAIN} {
  encode gzip

  # Route API traffic to the .NET container.
  handle /api/* {
    reverse_proxy dotnet:8001
  }

  # Everything else goes to Next.js.
  reverse_proxy next:4000
}

