import { shareImageResponse } from './lib/shareImageResponse'

export const alt = 'URL Tracker — shorten and track custom links'
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

export default shareImageResponse
