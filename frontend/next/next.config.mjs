console.log('next config NODE_ENV:', process.env.NODE_ENV);
console.log('next config NODE_ENV_ARG:', process.env.NODE_ENV_ARG);

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;